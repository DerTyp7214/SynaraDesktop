# Synara D-Bus API Documentation

This document is automatically generated by the DbusDocProcessor KSP.

## Interface: `dev.dertyp.synara.Api`
Interface for interacting with Synara from external programs.

### Summary

| Method | Parameters | Returns | Description |
| :--- | :--- | :--- | :--- |
| [`GetServerInfo`](#getserverinfo) | - | [SynaraServerInfo](#synaraserverinfo) | Get information about the Synara server (host, port). |
| [`GetPlaybackState`](#getplaybackstate) | - | [SynaraPlaybackState](#synaraplaybackstate) | Get the current playback state (position, duration, isPlaying). |
| [`GetCurrentSong`](#getcurrentsong) | - | [UserSong](#usersong) | Get metadata of the currently playing song. |
| [`Search`](#search) | `query` (String)<br>`options` (Map<[SearchFilter](#searchfilter), Variant<Any>>) | [SynaraSearchResult](#synarasearchresult) | Search for music and playlists. |
| [`GetCurrentLyrics`](#getcurrentlyrics) | - | String | Get the lyrics of the currently playing song. |
| [`GetQueue`](#getqueue) | `limit` (Int) | List<[SynaraQueueItem](#synaraqueueitem)> | Get the upcoming songs in the queue. |
| [`PlayQueueItem`](#playqueueitem) | `queueId` (String) | Unit | Play a specific item from the queue by its unique queueId. |
| [`PlayNext`](#playnext) | `id` (String)<br>`type` (String) | Unit | Insert an item to play immediately after the current song. |
| [`AddToQueue`](#addtoqueue) | `id` (String)<br>`type` (String) | Unit | Append an item to the end of the queue. |
| [`PlaySong`](#playsong) | `id` (String) | Unit | Play a specific song by its UUID. |
| [`PlayAlbum`](#playalbum) | `id` (String) | Unit | Play a specific album by its UUID. |
| [`PlayArtist`](#playartist) | `id` (String) | Unit | Play all songs by a specific artist. |
| [`PlayPlaylist`](#playplaylist) | `id` (String) | Unit | Play a specific playlist by its UUID. |

### Methods

#### `GetServerInfo`
Get information about the Synara server (host, port).

**Example Usage:**
```shell
dbus-send --session --print-reply=literal --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.GetServerInfo  | jq .
```

#### `GetPlaybackState`
Get the current playback state (position, duration, isPlaying).

**Example Usage:**
```shell
dbus-send --session --print-reply=literal --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.GetPlaybackState  | jq .
```

#### `GetCurrentSong`
Get metadata of the currently playing song.

**Example Usage:**
```shell
dbus-send --session --print-reply=literal --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.GetCurrentSong  | jq .
```

#### `Search`
Search for music and playlists.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `query` | String | The search query. |
| `options` | Map<[SearchFilter](#searchfilter), Variant<Any>> | Search filters. (Valid keys: [SearchFilter](#searchfilter)) |

**Example Usage:**
```shell
dbus-send --session --print-reply=literal --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.Search string:"Radiohead" dict:string:variant:"includeSongs",variant:boolean:false | jq .
```

#### `GetCurrentLyrics`
Get the lyrics of the currently playing song.

**Example Usage:**
```shell
dbus-send --session --print-reply=literal --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.GetCurrentLyrics 
```

#### `GetQueue`
Get the upcoming songs in the queue.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `limit` | Int | The maximum number of songs to return. |

**Example Usage:**
```shell
dbus-send --session --print-reply=literal --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.GetQueue int32:10 | jq .
```

#### `PlayQueueItem`
Play a specific item from the queue by its unique queueId.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `queueId` | String | The unique identifier of the queue item. |

**Example Usage:**
```shell
dbus-send --session --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.PlayQueueItem string:"123456789"
```

#### `PlayNext`
Insert an item to play immediately after the current song.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `id` | String | The UUID of the item. |
| `type` | String | The type of the item (song, album, artist, playlist). |

**Example Usage:**
```shell
dbus-send --session --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.PlayNext string:"album-uuid" string:"album"
```

#### `AddToQueue`
Append an item to the end of the queue.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `id` | String | The UUID of the item. |
| `type` | String | The type of the item (song, album, artist, playlist). |

**Example Usage:**
```shell
dbus-send --session --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.AddToQueue string:"song-uuid" string:"song"
```

#### `PlaySong`
Play a specific song by its UUID.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `id` | String | The UUID of the song. |

**Example Usage:**
```shell
dbus-send --session --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.PlaySong string:"song-uuid"
```

#### `PlayAlbum`
Play a specific album by its UUID.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `id` | String | The UUID of the album. |

**Example Usage:**
```shell
dbus-send --session --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.PlayAlbum string:"album-uuid"
```

#### `PlayArtist`
Play all songs by a specific artist.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `id` | String | The UUID of the artist. |

**Example Usage:**
```shell
dbus-send --session --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.PlayArtist string:"artist-uuid"
```

#### `PlayPlaylist`
Play a specific playlist by its UUID.

**Parameters:**

| Name | Type | Description |
| :--- | :--- | :--- |
| `id` | String | The UUID of the playlist. |

**Example Usage:**
```shell
dbus-send --session --dest=org.mpris.MediaPlayer2.synara /dev/dertyp/synara dev.dertyp.synara.Api.PlayPlaylist string:"playlist-uuid"
```

---
## Data Models

### Album

| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | PlatformUUID |  |
| `name` | String |  |
| `artists` | List<[Artist](#artist)> |  |
| `songCount` | Int |  |
| `releaseDate` | PlatformLocalDate? |  |
| `totalDuration` | Long |  |
| `totalSize` | Long |  |
| `coverId` | PlatformUUID? |  |
| `genres` | List<[Genre](#genre)> |  |
| `originalId` | String? |  |
| `musicbrainzId` | PlatformUUID? |  |

### Artist

| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | PlatformUUID |  |
| `name` | String |  |
| `isGroup` | Boolean |  |
| `artists` | List<[Artist](#artist)> |  |
| `about` | String |  |
| `genres` | List<[Genre](#genre)> |  |
| `imageId` | PlatformUUID? |  |
| `musicbrainzId` | PlatformUUID? |  |
| `isFollowed` | Boolean |  |

### Genre

| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | PlatformUUID |  |
| `name` | String |  |

### SearchFilter

| Value | Description |
| :--- | :--- |
| `includeSongs` | Whether to include songs in the search results. |
| `includeAlbums` | Whether to include albums in the search results. |
| `includeArtists` | Whether to include artists in the search results. |
| `includePlaylists` | Whether to include playlists in the search results. |

### SynaraPlaybackState

| Field | Type | Description |
| :--- | :--- | :--- |
| `isPlaying` | Boolean | Whether music is currently playing. |
| `position` | Long | Current playback position in milliseconds. |
| `duration` | Long | Total duration of the current song in milliseconds. |

### SynaraQueueItem

| Field | Type | Description |
| :--- | :--- | :--- |
| `queueId` | String | The unique identifier for this occurrence of the song in the queue. |
| `song` | [UserSong](#usersong) | Metadata of the song in the queue. |

### SynaraSearchResult

| Field | Type | Description |
| :--- | :--- | :--- |
| `songs` | List<[UserSong](#usersong)> | List of songs found in the search. |
| `albums` | List<[Album](#album)> | List of albums found in the search. |
| `artists` | List<[Artist](#artist)> | List of artists found in the search. |
| `playlists` | List<[UserPlaylist](#userplaylist)> | List of playlists found in the search. |

### SynaraServerInfo

| Field | Type | Description |
| :--- | :--- | :--- |
| `host` | String | The host of the Synara server. |
| `port` | Int | The port of the Synara server. |

### UserPlaylist

| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | PlatformUUID |  |
| `name` | String |  |
| `songs` | List<PlatformUUID> |  |
| `songEntries` | List<[UserPlaylistSong](#userplaylistsong)>? |  |
| `totalDuration` | Long |  |
| `imageId` | PlatformUUID? |  |
| `creator` | PlatformUUID |  |
| `description` | String |  |
| `origin` | String? |  |
| `modifiedAt` | PlatformDate? |  |

### UserPlaylistSong

| Field | Type | Description |
| :--- | :--- | :--- |
| `songId` | PlatformUUID |  |
| `addedAt` | Long |  |
| `musicBrainzId` | PlatformUUID? |  |

### UserSong

| Field | Type | Description |
| :--- | :--- | :--- |
| `id` | PlatformUUID |  |
| `title` | String |  |
| `artists` | List<[Artist](#artist)> |  |
| `album` | [Album](#album)? |  |
| `duration` | Long |  |
| `explicit` | Boolean |  |
| `releaseDate` | PlatformLocalDate? |  |
| `lyrics` | String |  |
| `path` | String |  |
| `originalUrl` | String |  |
| `trackNumber` | Int |  |
| `discNumber` | Int |  |
| `copyright` | String |  |
| `sampleRate` | Int |  |
| `bitsPerSample` | Int |  |
| `bitRate` | Long |  |
| `fileSize` | Long |  |
| `coverId` | PlatformUUID? |  |
| `musicBrainzId` | PlatformUUID? |  |
| `genres` | List<[Genre](#genre)> |  |
| `isFavourite` | Boolean? |  |
| `userSongCreatedAt` | PlatformDate? |  |
| `userSongUpdatedAt` | PlatformDate? |  |

